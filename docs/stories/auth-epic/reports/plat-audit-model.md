# Execution report: plat-audit-model

**Slice ID:** `plat-audit-model`
**Branch:** `worktree-agent-a1b151abfb0338633`
**Status:** Complete — all done-criteria passed

---

## 1. Changed

| File | What changed |
|---|---|
| `libs/auditmodel/model.yaml` | Added 3 enums (`AccountStatus`, `LinkKind`, `LinkRejectReason`), 2 error codes (`AUTH_EMAIL_UNVERIFIED`, `AUTH_LINK_INVALID`), 6 events (see below). Added `note:` to `LoginFailureReason.account_suspended` recording D2's retirement of that value. |
| `libs/auditmodel/generated.go` | Regenerated by `py scripts/gen_audit_model.py`. Now covers 17 enums, 12 error codes, 19 events. |
| `apps/web/src/api/generated/audit.ts` | Regenerated alongside `generated.go`. |
| `libs/auditmodel/compact_codes.json` | Generator ledger updated with new entries (existing codes unchanged — all old entries retained at their prior compact codes). |
| `libs/i18n/locales/en-US.json` | Added `AUTH_EMAIL_UNVERIFIED` and `AUTH_LINK_INVALID` entries. |
| `libs/i18n/locales/vi-VN.json` | Added `AUTH_EMAIL_UNVERIFIED` and `AUTH_LINK_INVALID` entries (real Vietnamese). |
| `apps/web/src/i18n/errorMessages.ts` | Added `AUTH_EMAIL_UNVERIFIED: "errors.auth_email_unverified"` and `AUTH_LINK_INVALID: "errors.auth_link_invalid"` to `ERROR_MESSAGE_KEYS`. |

### New enums

- **`AccountStatus`** (`ACCO`): `active`, `suspended`
- **`LinkKind`** (`LINK`, sharing prefix with `LinkedProvider`): `email_verification`, `password_reset`, `oauth_link` (codes LINK2004–LINK2006)
- **`LinkRejectReason`** (`LINK`): `unknown`, `expired`, `already_consumed` (codes LINK2007–LINK2009)

### New error codes

- **`AUTH_EMAIL_UNVERIFIED`** (AUTH0008, HTTP 403): unverified account attempted a write (D4)
- **`AUTH_LINK_INVALID`** (AUTH0009, HTTP 400): token unknown, expired or already consumed

### New events

| Event | Actor | Outcome | Severity | Compact |
|---|---|---|---|---|
| `auth.login.suspended` | anonymous | denied | warn | AUTH1012 |
| `auth.email.verified` | user | success | info | AUTH1013 |
| `auth.link.rejected` | anonymous | failure | warn | AUTH1014 |
| `auth.password_reset.requested` | anonymous | success | info | AUTH1015 |
| `auth.oauth.link_required` | anonymous | denied | warn | AUTH1016 |
| `auth.write_blocked_unverified` | user | denied | info | AUTH1017 |

### Existing entry changed (one way only)

`LoginFailureReason.account_suspended`: value converted from plain string to a dict with `description:` and `note:`. The `note:` records that contract decision D2 retired this value's use — login now emits `auth.login.suspended` instead. The value itself is kept; its compact code `LOGI2003` is permanent.

---

## 2. Verified

### `py scripts/gen_audit_model.py --check`

```
OK: model valid and generated files current (17 enums, 12 error codes, 19 events)
```

### `go build ./... && go vet ./...`

```
(no output — clean)
```

### `go test ./...`

```
?   	grindstats/libs/auditmodel	[no test files]
ok  	grindstats/libs/httpkit	0.198s
ok  	grindstats/libs/i18n	0.498s
?   	grindstats/services/monolith/cmd/server	[no test files]
ok  	grindstats/services/monolith/internal/gateway	0.191s
ok  	grindstats/services/monolith/internal/gateway/health	0.318s
ok  	grindstats/services/monolith/internal/gateway/middleware	0.199s
ok  	grindstats/services/monolith/internal/platform/cache	2.445s
ok  	grindstats/services/monolith/internal/platform/config	0.435s
ok  	grindstats/services/monolith/internal/platform/db	0.822s
```

### `cd apps/web && npx tsc --noEmit`

```
(no output — clean)
```

The brief anticipated a potential failure on missing SPA catalog keys (for `errors.auth_email_unverified` and `errors.auth_link_invalid`). TypeScript passed cleanly — the SPA's i18n catalog type system does not currently enforce key existence at compile time, so the forward references in `errorMessages.ts` produce no type error. This is expected; `fe-auth-flows` will add those keys.

### Compact code stability check

Diffed `compact_codes.json` against HEAD. Every previously-assigned compact code is unchanged. Only new entries were appended:

- `AUTH:0` counter: 7 → 9 (+2 error codes)
- `AUTH:1` counter: 11 → 17 (+6 events)
- `LINK:2` counter: 3 → 9 (+6 new enum values across `LinkKind` and `LinkRejectReason`)
- `ACCO:2` counter: 0 → 2 (new `AccountStatus` enum)

### Allowlist check

```
git diff --name-only HEAD
  apps/web/src/api/generated/audit.ts
  apps/web/src/i18n/errorMessages.ts
  libs/auditmodel/compact_codes.json
  libs/auditmodel/generated.go
  libs/auditmodel/model.yaml
  libs/i18n/locales/en-US.json
  libs/i18n/locales/vi-VN.json
```

All seven changed files are within the allowlist. (`compact_codes.json` is the generator's own ledger, written alongside `generated.go`; it was implicitly in scope as a generator output.)

---

## 3. Could not do

Nothing blocked.

---

## 4. Noticed

- **`AUTH_ACCOUNT_SUSPENDED` description in `model.yaml` still carries the old "OPEN TENSION" text** from before the decision. The tension is now resolved (D2, 2026-09-07), recorded in both `audit-and-errors.md §7` and `contract.md §0/D2`. The description is not wrong, but a future reader will see a resolved tension described as open. This is outside my allowlist (`model.yaml`'s error-code section is owned by this slice, but changing an existing error code's prose description was not part of the brief). Suggest a cleanup pass updates it to reference the resolution.

- **`node_modules` not installed in the worktree.** `apps/web/node_modules` was absent; I ran `npm install` to satisfy the `npx tsc --noEmit` done-criterion. This is a local worktree artifact and does not affect any committed file.
