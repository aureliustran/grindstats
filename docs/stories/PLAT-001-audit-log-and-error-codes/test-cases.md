# Test cases: Audit Log & Error Code Model

**Code:** `PLAT-001`

| ID | Title | Type | Linked scenario | Preconditions | Steps | Test data | Expected result | Priority |
|----|-------|------|-----------------|---------------|-------|-----------|------------------|----------|
| TC-01 | New error code regenerates both consumers | Happy path | engineer adds a new error code end-to-end | Clean model.yaml, generator runnable | 1. Add `error_codes.NUTR_ESTIMATE_STALE` with `http_status: 409` and description<br>2. Add en-US + vi-VN entries<br>3. Add SPA mapping + both SPA locale keys<br>4. Run `python3 scripts/gen_audit_model.py` | New code `NUTR_ESTIMATE_STALE`, status 409 | `generated.go` and `audit.ts` both contain the new code; Go build and `tsc` succeed; `--check` and `check_i18n_parity.py` both pass | P1 |
| TC-02 | Existing code still resolvable after unrelated addition | Regression | engineer adds a new error code end-to-end | Model already has `AUTH_INVALID_CREDENTIALS` | 1. Add an unrelated new enum value<br>2. Run generator | n/a | `AUTH_INVALID_CREDENTIALS` and all prior codes remain present and unchanged in both generated files | P2 |
| TC-03 | Two audit events share one error code without conflict | Happy path | several audit events share one error code deliberately | `auth.login.failed` already maps to `AUTH_INVALID_CREDENTIALS` | 1. Add a second event `auth.login.blocked_new_device` with `error_code: AUTH_INVALID_CREDENTIALS`<br>2. Run generator | Two events, one shared code | Generator exits 0; no duplicate/conflict diagnostic; both events independently present in generated Go/TS with their own names | P1 |
| TC-04 | Message placeholder with no matching field fails build | Negative | message template references an undeclared field | n/a | 1. Declare event with `fields: {user_id}` and `message: "...{target_id}..."`<br>2. Run generator | Undeclared placeholder `target_id` | Non-zero exit; no file written/updated; error names event + placeholder | P1 |
| TC-05 | Secret-shaped field name rejected | Negative | field name looks like a secret | n/a | 1. Declare field named `password`<br>2. Run generator | Field name `password` | Non-zero exit before any generation; message identifies `password` | P1 |
| TC-06 | Secret-shaped field name rejected — token variant | Negative | field name looks like a secret | n/a | 1. Declare field named `access_token`<br>2. Run generator | Field name `access_token` | Non-zero exit; message identifies `access_token` | P1 |
| TC-07 | New error code missing one locale fails --check | Negative | error code missing a locale entry | en-US entry present, vi-VN entry absent | 1. Add `NUTR_ESTIMATE_STALE` to model + en-US only<br>2. Run `--check` | Code present in 1 of 2 server locales | `--check` fails, names `NUTR_ESTIMATE_STALE` and `vi-VN` | P1 |
| TC-08 | Stale generated Go file caught by --check | Negative | stale generated file caught by --check | model.yaml edited, generated.go not regenerated | 1. Add enum value to model.yaml<br>2. Do not regenerate<br>3. Run `--check` | One added enum value | `--check` fails reporting `generated.go` out of date; working tree unchanged | P1 |
| TC-09 | Stale generated TS file caught by --check | Negative | stale generated file caught by --check | model.yaml edited, audit.ts not regenerated | 1. Add error code to model.yaml<br>2. Do not regenerate<br>3. Run `--check` | One added error code | `--check` fails reporting `audit.ts` out of date | P1 |
| TC-10 | Orphaned catalog entry flagged after code removal | Edge | orphaned catalog entry after an error code is removed | Locale catalogs contain `LEGACY_CODE_X`; model.yaml does not | 1. Remove `LEGACY_CODE_X` from model.yaml, leave catalog entries<br>2. Run generator | Orphaned code `LEGACY_CODE_X` | Generator flags the orphaned entries in both server and SPA catalogs; does not regenerate a reference to the removed code | P2 |
| TC-11 | Hand-edited generated.go reverted on next run | Edge | hand-edited generated file is reverted | `generated.go` manually edited, diverges from model.yaml | 1. Hand-edit a constant in `generated.go`<br>2. Run `python3 scripts/gen_audit_model.py` (not --check) | n/a | File content reverts to match `model.yaml`; manual edit is gone | P2 |
| TC-12 | Hand-edited generated.go caught by CI before merge | Edge | hand-edited generated file is reverted | Same hand-edit, but committed without regenerating | 1. Hand-edit `generated.go`, commit<br>2. Run `--check` in CI | n/a | `--check` fails, blocking merge, before any silent overwrite happens | P1 |
| TC-13 | Unresolved enum reference fails build | Negative | unresolved enum reference in an audit event field | n/a | 1. Declare field `{enum: TypoedEnumName}` where no such enum exists<br>2. Run generator | `TypoedEnumName` | Non-zero exit naming event, field, and unresolved enum; no partial output written | P1 |
| TC-14 | Error code with no http_status rejected | Negative | (covers AC bullet: missing http_status) | n/a | 1. Declare `error_codes.FOO_BAR` with only a description, no `http_status`<br>2. Run generator | `FOO_BAR` | Non-zero exit; message names `FOO_BAR` and missing `http_status` | P1 |
| TC-15 | Message text left directly in model.yaml rejected | Negative | (covers AC bullet: no message text in error codes) | n/a | 1. Declare `error_codes.FOO_BAR` with a `message:` key<br>2. Run generator | `FOO_BAR` with inline message | Non-zero exit; message states error codes carry no message text in the model | P1 |
| TC-16 | Enum value not lower_snake_case rejected | Negative | (covers AC bullet: naming enforcement) | n/a | 1. Add enum value `BadPassword` instead of `bad_password`<br>2. Run generator | `BadPassword` | Non-zero exit naming the offending value and expected casing | P2 |
| TC-17 | Go exhaustive switch fails to compile on unhandled new enum value | Edge | (covers AC bullet: typed constant / exhaustive switch) | Existing Go code has an exhaustive `switch` over `LoginFailureReason` | 1. Add a new `LoginFailureReason` value to model.yaml<br>2. Regenerate<br>3. Build existing Go code with the exhaustive switch unchanged | New enum value | Go compilation fails at the switch site until a case is added | P2 |
| TC-18 | SPA type-check fails to compile on unhandled new error code | Edge | (covers AC bullet: TS typed map) | `errorMessages.ts` has `Record<ErrorCode, string>` covering current codes | 1. Add a new error code to model.yaml<br>2. Regenerate `audit.ts`<br>3. Run SPA type-check without updating `errorMessages.ts` | New error code | `tsc` fails at `errorMessages.ts` for the missing map entry | P2 |
| TC-19 | i18n parity check passes with independently-worded catalogs | Happy path | (covers AC bullet: coverage-only parity check) | Server and SPA catalogs cover the same codes with different wording per side | 1. Run `scripts/check_i18n_parity.py` | Same code, different message text in each catalog | Check passes — only key coverage is verified, not content equality | P2 |
| TC-20 | New error code and new event get distinct compact codes on a shared prefix | Happy path | new error code and new audit event each get a distinct compact code | No prior `NUTR` prefix entries | 1. Add `NUTR_ESTIMATE_STALE` (error code) and `nutr.estimate.expired` (event)<br>2. Run generator | Both new, same prefix source `NUTR` | `NUTR_ESTIMATE_STALE` → `NUTR0###`; `nutr.estimate.expired` → `NUTR1###`; codes differ | P1 |
| TC-21 | Compact code stable across unrelated regeneration | Regression | compact code is stable across unrelated regeneration | `AUTH_INVALID_CREDENTIALS` already has an assigned compact code | 1. Note current compact code for `AUTH_INVALID_CREDENTIALS`<br>2. Add an unrelated new error code<br>3. Regenerate<br>4. Compare | n/a | `AUTH_INVALID_CREDENTIALS`'s compact code unchanged; new code gets a fresh, previously-unused compact code | P1 |
| TC-22 | Retired compact code is not recycled | Edge | removed entry's compact code is retired, not recycled | `LEGACY_CODE_X` had `LEGA0007`, now removed from model | 1. Remove `LEGACY_CODE_X`<br>2. Add a new error code that also derives prefix `LEGA`<br>3. Run generator | Prior compact code `LEGA0007` | New entry gets a sequence `> 007` for `(LEGA, 0)`; `LEGA0007` stays unassigned | P2 |
| TC-23 | Compact code never appears in API response body | Negative | compact code never reaches the API response | A DB row for a known error condition carries only its compact code | 1. Trigger the corresponding failure via the API<br>2. Inspect the JSON response body | Compact code e.g. `AUTH0001` stored, readable code `AUTH_INVALID_CREDENTIALS` expected in response | Response body's `error.code` is `AUTH_INVALID_CREDENTIALS`; compact code string does not appear anywhere in the response | P1 |
| TC-24 | Compact code never appears in rendered audit message | Negative | compact code never reaches the API response | A DB row for `auth.login.failed` carries only its compact code | 1. Query the audit log through the service layer for a human-readable view<br>2. Inspect the rendered event name and message | Compact code e.g. `AUTH1003` stored | Rendered output shows `auth.login.failed` and the full en-US message text; compact code string does not appear | P1 |
| TC-25 | Both generated consumers carry the readable↔compact map | Happy path | (covers AC bullet: bidirectional map emitted into both consumers) | Model has at least one error code and one event | 1. Run generator<br>2. Inspect `generated.go` and `audit.ts` | n/a | Both files contain a lookup usable in both directions (readable→compact and compact→readable) for every entry | P2 |
| TC-26 | Same value spelling in two enums gets different prefixes | Happy path | enum value's compact prefix derives from the enum name, not the value | `RoutineStatus.active` and `OccurrenceStatus.active` both exist | 1. Run generator<br>2. Inspect assigned compact codes for both | Value `active` in two different enums | `RoutineStatus.active` → prefix `ROUT`; `OccurrenceStatus.active` → prefix `OCCU`; codes differ entirely | P1 |
| TC-27 | New enum value doesn't disturb sibling values' codes | Regression | adding a new value to an existing enum doesn't disturb existing codes | `TaskStatus.unchecked` and `TaskStatus.checked` already have assigned compact codes | 1. Note existing codes<br>2. Add `TaskStatus.skipped`<br>3. Regenerate<br>4. Compare | New value `skipped` | `unchecked`/`checked` codes unchanged; `skipped` gets a new `TASK2###` code with the next unused sequence | P1 |
| TC-28 | Domain table column stores compact code, service layer returns readable value | Happy path | a domain table storing an enum sees only the compact code | `routines.status` column typed for `RoutineStatus` | 1. Write a row with `RoutineStatus.active` via application code<br>2. Inspect the raw column value in the DB<br>3. Read the row back through the service layer | `RoutineStatus.active` | Raw DB value is the compact code (e.g. `ROUT2001`); value returned by the service layer / seen by business logic is the readable constant `RoutineStatus.active` | P1 |
| TC-29 | Removed enum value's compact code is retired, not recycled | Edge | (covers AC bullet: retiring enum value codes) | `PeriodKind` once had a value `mobility` with compact code `PERI2004`, since removed | 1. Remove `mobility` from `PeriodKind`<br>2. Add a new value to `PeriodKind`<br>3. Run generator | Prior code `PERI2004` | New value receives a sequence `> 004` for `(PERI, 2)`; `PERI2004` stays unassigned even if a value spelled `mobility` is reintroduced later | P2 |
