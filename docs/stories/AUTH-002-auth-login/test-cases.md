# Test cases: Login and session establishment

| ID | Title | Type | Linked scenario | Preconditions | Steps | Test data | Expected result | Priority |
|----|-------|------|-----------------|---------------|-------|-----------|-----------------|----------|
| TC-01 | Successful login issues httpOnly cookies and a CSRF token | Happy path | successful login issues cookie-only tokens and a CSRF token | Verified account exists | 1. `POST /login`<br>2. Inspect response headers/body<br>3. Check `document.cookie` in a browser context | `returning@example.com` / correct password | Cookies set httpOnly/Secure/SameSite=Lax; refresh path-scoped; CSRF token in body; no token strings in body; `document.cookie` empty of auth tokens | P1 |
| TC-02 | Session record captures device label | Happy path | login creates a session record with a device label | Account exists | 1. Log in with a known User-Agent string<br>2. `GET /sessions` | Chrome-on-macOS UA string | New session entry with a parsed device label and created-at timestamp | P2 |
| TC-03 | Google login for an existing linked account matches local login's session shape | Happy path | Google OAuth login for an existing account issues the same session shape | Account previously linked to Google identity | 1. Complete Google OAuth<br>2. Compare resulting cookies/CSRF/session record to TC-01's | Linked Google test account | Identical cookie flags, CSRF presence, and session record shape as email/password login | P1 |
| TC-04 | Wrong password returns a generic failure | Negative | wrong password gives a generic failure without revealing which factor failed | Account exists | 1. `POST /login` with correct email, wrong password | `returning@example.com` / `wrong-pw` | Generic error; no cookies set; failure counter incremented | P1 |
| TC-05 | Unknown email returns an identical generic failure | Negative | nonexistent email gives an identical generic failure | No account for the email | 1. `POST /login` with unknown email + any password | `nobody@example.com` | Response byte-identical in text and status to TC-04 | P1 |
| TC-06 | Fifth consecutive failure triggers 30s backoff | Negative | five consecutive failures trigger backoff | 4 prior consecutive failures recorded | 1. Fail login a 5th time<br>2. Immediately retry | Wrong password ×5 | 5th failure recorded; immediate retry rejected with an explained delay of ≥30s | P1 |
| TC-07 | Backoff grows on continued failures | Negative | backoff increases with continued failures | Prior 30s backoff already served | 1. Fail again after the first backoff window | Wrong password | Required delay before next attempt exceeds 30s | P2 |
| TC-08 | Successful login resets the failure counter | Edge | a successful login resets the failure counter | 3 consecutive failures recorded, below threshold | 1. Log in successfully<br>2. Fail once more<br>3. Confirm no backoff yet | Correct credentials, then 1 wrong attempt | Counter reset to 0 on success; single subsequent failure does not trigger backoff | P2 |
| TC-09 | Backoff applies across source IPs for the same account | Negative | backoff blocks the account even from a new source IP | Account in active backoff window from IP A | 1. Attempt login for the same account from IP B during the window | Same account, different IP | Attempt from IP B is also subject to the remaining backoff | P2 |
| TC-10 | Unverified account can still log in | Edge | an unverified account can still log in | Account registered, not verified | 1. Log in with correct credentials | Unverified account | Login succeeds; normal session established | P2 |
| TC-11 | Refresh cookie is inaccessible outside its path scope | Negative | successful login issues cookie-only tokens and a CSRF token | Logged in | 1. Inspect refresh cookie's `Path` attribute<br>2. Confirm it is not sent on a request outside `/api/v1/auth` | — | `Path=/api/v1/auth`; browser omits it on unrelated API calls | P2 |
