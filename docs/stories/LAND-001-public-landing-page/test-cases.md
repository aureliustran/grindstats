# Test cases: Public landing page for unauthenticated visitors

Every case traces to a scenario in `acceptance-criteria.md`. Priorities: **P1** breaks the
core convert-or-sign-in flow or leaks information; **P2** degrades an important path;
**P3** cosmetic or rare.

| ID | Title | Type | Linked scenario | Preconditions | Steps | Test data | Expected result | Priority |
|----|-------|------|-----------------|---------------|-------|-----------|-----------------|----------|
| TC-01 | Landing page renders hero and marketing menu | Happy path | unauthenticated visitor sees the landing page and its menu | No session cookies present | 1. Load `/`<br>2. Inspect hero and header | — | Regimen lines, "EVERY SINGLE DAY" punchline and silhouette render; header shows Features / How it works / Pricing + Log in / Get started | P1 |
| TC-02 | No app destinations exposed to visitors | Negative | unauthenticated visitor sees the landing page and its menu | No session cookies present | 1. Load `/`<br>2. Search rendered DOM for links to authenticated routes | — | No link or control targets `/dashboard`, `/routines`, `/nutrition` or any authenticated route | P1 |
| TC-03 | Hamburger menu opens and closes on mobile | Happy path | menu collapses to a hamburger on a narrow viewport | Viewport below the mobile breakpoint | 1. Load `/`<br>2. Activate hamburger<br>3. Select an anchor | Viewport 375×812 | Anchors hidden initially; expand on activation; menu collapses after selecting an item | P2 |
| TC-04 | Anchor navigation reaches the target section | Happy path | menu anchor scrolls to the matching section | Desktop viewport | 1. Load `/`<br>2. Select "How it works" | — | Viewport scrolls to the "How it works" section | P2 |
| TC-05 | Reduced motion disables animated scrolling | Edge | menu anchor scrolls to the matching section | OS/browser `prefers-reduced-motion: reduce` | 1. Load `/`<br>2. Select "How it works" | `prefers-reduced-motion: reduce` | Section is reached without animated scrolling or hero motion effects | P3 |
| TC-06 | Registration with a new email reaches the dashboard | Happy path | new visitor registers and reaches the dashboard | No account for the test email | 1. Open modal → Sign up<br>2. Submit valid email + password<br>3. Observe redirect | `new+<timestamp>@example.com` / `Str0ng-Passw0rd!` | Account created, session cookies set, redirected to dashboard, modal dismissed | P1 |
| TC-07 | Login with valid credentials reaches the dashboard | Happy path | returning visitor logs in and reaches the dashboard | Account exists and is verified | 1. Open modal → Log in<br>2. Submit correct credentials | `returning@example.com` / correct password | Session cookies set; redirected to dashboard | P1 |
| TC-08 | Google OAuth completes and authenticates | Happy path | visitor authenticates with Google | Google test account available; OAuth configured | 1. Open modal<br>2. Select Continue with Google<br>3. Approve consent | Google test account | Returned to app with session cookies set; redirected to dashboard | P1 |
| TC-09 | Google OAuth cancellation leaves no session | Negative | cancelling Google authorization returns the visitor without a session | Google test account available | 1. Select Continue with Google<br>2. Deny consent | — | Returned to landing page; no session cookies; non-blocking message shown; email/password still usable | P1 |
| TC-10 | Modal dismisses without losing scroll position or focus | Edge | auth modal opens over the page and can be dismissed without losing position | Desktop viewport | 1. Scroll to Features<br>2. Activate Log in<br>3. Press Escape | — | Modal closes, no navigation occurred, page still at Features, focus back on the Log in control | P2 |
| TC-11 | Focus is trapped inside the open modal | Edge | focus stays inside the modal while it is open | Modal open | 1. Tab forward past the last focusable element<br>2. Shift-Tab backward past the first | — | Focus wraps within the modal in both directions and never reaches page content behind it | P2 |
| TC-12 | Tab switch clears the other tab's error state | Edge | switching modal tabs clears errors from the other tab | Modal on Log in showing a failed-login error | 1. Switch to Sign up tab<br>2. Inspect fields and messages | — | Login error cleared; Sign up fields empty with no validation messages | P3 |
| TC-13 | Authenticated visitor is redirected off the landing page | Edge | already-authenticated visitor is redirected away from the landing page | Valid session cookies present | 1. Navigate to `/` | Valid session | Redirected to dashboard; landing page is not the final rendered destination | P2 |
| TC-14 | Wrong password returns a generic failure | Negative | invalid credentials do not reveal whether the email is registered | Account exists for the email | 1. Open modal → Log in<br>2. Submit correct email, wrong password | `returning@example.com` / `wrong-password` | Generic "Email or password is incorrect"; password field cleared, email retained; no session set | P1 |
| TC-15 | Unknown email returns an identical generic failure | Negative | invalid credentials do not reveal whether the email is registered | No account for the email | 1. Open modal → Log in<br>2. Submit unknown email + any password | `nobody@example.com` / `any-password` | Message and HTTP status are byte-identical to TC-14 — no enumeration signal via text, status or timing | P1 |
| TC-16 | Duplicate registration does not confirm the account exists | Negative | registering an already-registered email does not confirm the account exists | Account exists for the email | 1. Open modal → Sign up<br>2. Submit that email with a valid password | `taken@example.com` / `Str0ng-Passw0rd!` | Response does not state the email is taken; same neutral next step as a successful registration | P1 |
| TC-17 | Malformed email is blocked before any request | Negative | client-side validation blocks a malformed submission before any request | Modal open on Sign up; network panel recording | 1. Enter `not-an-email`<br>2. Attempt to submit | `not-an-email` | Field-level error shown; submit disabled; no request issued to auth-service | P2 |
| TC-18 | Password below policy minimum is blocked client-side | Negative | client-side validation blocks a malformed submission before any request | Modal open on Sign up | 1. Enter a valid email and a too-short password<br>2. Attempt to submit | `new@example.com` / `abc` | Password validation message shown; submit disabled; no request issued | P2 |
| TC-19 | Auth-service outage ends the loading state with an error | Negative | auth-service being unreachable surfaces an error instead of hanging | auth-service stopped or network stubbed to fail | 1. Open modal → Log in<br>2. Submit valid credentials | Valid credentials | Spinner resolves, retry message shown, modal stays open with email preserved; no indefinite loading | P1 |
| TC-20 | Rate limiting is reported and enforced in the UI | Negative | rate-limited login attempts are reported clearly | Login rate limit configured per NFR-05 | 1. Submit failed logins until the limit is exceeded<br>2. Submit once more | Repeated wrong passwords | 429 handled with a "too many attempts" message; submit disabled for the cool-down period | P2 |
| TC-21 | Page renders correctly in dark theme | Edge | unauthenticated visitor sees the landing page and its menu | — | 1. Set dark theme<br>2. Load `/` and open the modal | `prefers-color-scheme: dark` | Hero, sections, menu and modal all legible; contrast meets WCAG AA; no unstyled or invisible elements | P2 |
| TC-22 | Silhouette asset is original and depicts no real or existing figure | Regression | (asset constraint, story.md — Dependencies / assumptions) | Asset committed to the repo | 1. Review the asset and its provenance record | — | Asset is an original GrindStats work, faceless, not modeled on any real person or existing fictional character; provenance noted alongside the file | P1 |
| TC-23 | Vietnamese browser receives the Vietnamese page | Happy path | a Vietnamese-language browser gets the Vietnamese landing page | Browser language set to Vietnamese | 1. Set `Accept-Language: vi-VN`<br>2. Load `/`<br>3. Open the auth modal | `Accept-Language: vi-VN,vi;q=0.9` | All copy, nav and modal labels in Vietnamese; `<html lang="vi-VN">`; no English string visible | P1 |
| TC-24 | Unsupported language falls back to English | Edge | any other language falls back to English | Browser language set to French | 1. Set `Accept-Language: fr-FR`<br>2. Load `/` | `Accept-Language: fr-FR` | Page renders `en-US`; `<html lang="en-US">` | P2 |
| TC-25 | Plain `vi` tag maps to the vi-VN catalog | Edge | a Vietnamese-language browser gets the Vietnamese landing page | Browser sends bare `vi` | 1. Set `Accept-Language: vi`<br>2. Load `/` | `Accept-Language: vi` | Vietnamese catalog used, not an English fallback | P2 |
| TC-26 | `?lng=vi` override works in a production build | Edge | the locale override forces Vietnamese for testing | Production build; browser set to English | 1. Load `/?lng=vi` | `?lng=vi` | Page renders in Vietnamese; override is not stripped from the production bundle | P1 |
| TC-27 | Vietnamese copy does not break layout at any breakpoint | Edge | Vietnamese text does not break any layout | Page rendering in vi-VN | 1. View at 375px, 768px and 1440px<br>2. Inspect hero, nav, buttons and modal errors | Longest vi-VN strings | No clipping, truncation or overlap; stacked diacritics fully visible | P1 |
| TC-28 | Every face renders Vietnamese glyphs | Edge | Vietnamese text does not break any layout | Fonts loaded | 1. Render `ơ ư đ ă â ê ô ế ộ ữ ằ ậ` in display, body and mono faces | Vietnamese diacritic set | Real glyphs in every face; no tofu boxes, no fallback-font mismatch | P1 |
| TC-29 | Locale catalogs are in parity | Regression | (i18n rule, docs/i18n-guidelines.md §2) | Both catalogs committed | 1. Run `python3 scripts/check_i18n_parity.py` | — | Exit code 0; every `en-US` key present in `vi-VN` and vice versa | P1 |
| TC-30 | No hardcoded user-facing strings | Regression | (i18n rule, docs/i18n-guidelines.md §1) | Landing page implemented | 1. Grep components for string literals in JSX text, `aria-label`, `alt`, `title`, `placeholder` | — | Every user-facing string resolves through a translation key | P1 |
| TC-31 | Numbers format per locale | Negative | (i18n rule, docs/i18n-guidelines.md §4) | Any rendered numeric value | 1. Render a value of 2620 in `en-US`<br>2. Render the same value in `vi-VN` | `2620` | `2,620` in en-US and `2.620` in vi-VN — separators inverted, via `Intl.NumberFormat`, never `toFixed()` | P1 |
| TC-32 | No raw design values in components | Regression | the page conforms to the token system | Landing page implemented | 1. Grep components and stylesheets for hex colors, `px` sizes and `ms` durations | — | No raw values; everything resolves to a token from `tokens.css` | P2 |
| TC-33 | Contrast floors met in both themes | Regression | the page conforms to the token system | Page implemented | 1. Run a contrast checker over the rendered page in light and dark theme | — | Body text ≥4.5:1; large text and meaningful non-text elements ≥3:1; `ink-3` never used for body copy | P1 |

