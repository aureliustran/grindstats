# Test cases: Viewing and revoking my own sessions

| ID | Title | Type | Linked scenario | Preconditions | Steps | Test data | Expected result | Priority |
|----|-------|------|-----------------|---------------|-------|-----------|-----------------|----------|
| TC-01 | Session list shows all devices with current one marked | Happy path | listing sessions shows every active device with the current one marked | Logged in on 2 devices | 1. `GET /sessions` from device A | 2 active sessions | Both listed with device label + created-at; device A marked current, device B not | P1 |
| TC-02 | Revoking one session leaves the other untouched | Happy path | revoking a specific session ends only that session | 2 active sessions, A and B | 1. `DELETE /sessions/{B}` from A<br>2. Attempt refresh from B<br>3. Attempt refresh from A | Session B's ID | B's refresh fails; A remains valid | P1 |
| TC-03 | Revoked session's access token stops working at or before natural expiry | Edge | a revoked session's access token stops working within its natural lifetime | Session revoked with ~10 min of access-token life remaining | 1. Revoke the session<br>2. Immediately retry a request with its still-unexpired access token | Revoked session's access token | Rejected immediately if blacklist is checked; rejected no later than the token's original expiry otherwise | P2 |
| TC-04 | Cannot revoke a session belonging to another user | Negative | a user cannot revoke another user's session | Two distinct users, each with a session | 1. As user 1, call `DELETE /sessions/{user 2's session id}` | Foreign session ID | Rejected (404/403); user 2's session unaffected | P1 |
| TC-05 | Session list is scoped strictly to the caller | Negative | listing sessions never includes another user's sessions | Multiple users with active sessions | 1. As user 1, call `GET /sessions` | — | Only user 1's own sessions returned | P1 |
| TC-06 | Listing sessions with only the current one active is not an error | Edge | an empty session list is possible without being an error | All other sessions ended; only current remains | 1. `GET /sessions` | — | 200 with exactly the current session listed, not an error or empty-list edge case | P3 |
